scopes:

    enabled:

        # Filter name
        label: rainlab.user::lang.locations.hide_disabled

        # Filter type
        type: checkbox

        # SQL Conditions
        conditions: is_enabled = 1