# ===================================
#  Field Definitions
# ===================================

fields:
    update_existing:
        label: Update existing users
        comment: Update users that already exist, matched by email address, instead of skipping them.
        type: checkbox

    auto_create_groups:
        label: Auto-create groups
        comment: Create user groups that do not yet exist when resolving the groups column.
        type: checkbox
